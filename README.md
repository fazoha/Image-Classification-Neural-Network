Created a simple Feed Forward neural network. Trained it to identify garment peices into one of the classes I defined it into. 
Trained it using the fashion MNIST dataset form the Tensorflow website.
